<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.iesb.mobile.rpg_pi2_20211">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RPG_PI2_20211">
        <activity android:name=".ui.activity.AppActivity"></activity>
        <activity android:name=".ui.activity.loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.antigos.PersonagemActivity" />
        <activity android:name=".ui.activity.antigos.HomeActivity" />
        <activity android:name=".ui.activity.antigos.CadastroActivity" />
        <activity android:name=".ui.activity.antigos.EsqueciActivity" />
    </application>

</manifest>